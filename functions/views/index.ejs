<!DOCTYPE html>
<html lang="en">
  <head>
    <title>My Best Todo List</title>
    <%- include('./common/meta') %>
    <%- include('./common/firebaseHeader') %>
    <%- include('./common/script') %>
    <script src="/web-components/list.js"></script>
    <script src="/web-components/todo-list-card.js"></script>
    <script src="/web-components/tag.js"></script>
    <script src="/web-components/section.js"></script>
    <%- include('./common/style') %>
    <style>
      mbtl-header + mbtl-section {
        margin-top: 5vw;
      }

      h1 + mbtl-list  {
        margin-top: 50px;
      }

      h2 + mbtl-list {
        margin-top: 20px;
      }

      h2 + p {
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <mbtl-header></mbtl-header>

    <mbtl-section>
      <h1>This Is My Best Todo List!</h1>

      <mbtl-list>
        <% for (var item of items) { %>
          <li>
            <mbtl-todo-list-card
              href="/item/<%= item.id %>"
              src="<%= item.imageUrl %>"
              title="<%= item.title %>"
              tags="<%= item.escapeTags %>"
            >
            </mbtl-todo-list-card>
          </li>
        <% } %>
      </mbtl-list>
    </mbtl-section>

    <mbtl-section class="white">
      <h2>Popular Tag!</h2>

      <mbtl-list>
        <% for (var tag of tags) { %>
        <li>
          <mbtl-tag
            href="/tags/<%= tag.id %>"
            tag="<%= tag.id %>"
            count="<%= tag.count %>"
          >
          </mbtl-tag>
        </li>
        <% } %>
      </mbtl-list>
    </mbtl-section>

    <mbtl-section>
      <h2>Introduction</h2>

      <p>This site was created to share the my best todo list that I created.</p>
      <p>Also created to learn from the todo list created by others.</p>
      <p>If you like, please post the created todo list.</p>
    </mbtl-section>
  </body>
</html>
